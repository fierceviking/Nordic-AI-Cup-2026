# Lower bounds rather than exact pins, deliberately: you will almost certainly
# be installing an ASR stack (torch, faster-whisper, ...) alongside these, and
# hard pins here would fight it. Verified against the versions in brackets.

fastapi>=0.115            # [0.127.0]
uvicorn>=0.30             # [0.40.0]
pydantic>=2.7,<3          # [2.12.5]

# Only needed by local_evaluator.py.
requests>=2.31            # [2.32.5]

# --------------------------------------------------------------------------- #
# The solution. All local; nothing here calls out to a cloud API.
# Verified on Python 3.12 with an RTX 5070 (sm_120), CUDA 12.8.
#
# torch is installed from the CUDA index, not PyPI:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# CTranslate2 does not ship cuBLAS/cuDNN; solution/asr.py adds torch's lib
# directory to the Windows DLL search path so faster-whisper can find them.
# --------------------------------------------------------------------------- #

torch>=2.6                # [2.11.0+cu128]
faster-whisper>=1.1       # [1.2.1]  ASR with word-level timestamps
transformers>=4.50        # [5.x]    NLI reader
accelerate>=1.0           # [1.15.0] device placement for transformers
sentence-transformers>=3  # [5.x]    dense retrieval
scikit-learn>=1.4         # [1.9.1]  experiment 3 only
rapidfuzz>=3.0            # [3.14.6] fuzzy term matching over noisy ASR
numpy>=1.26               # [2.5.3]
